{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) ;; *) exit 0;; esac; uv run ruff format \"$f\" >/dev/null 2>&1; }",
            "timeout": 60,
            "statusMessage": "ruff format on edited file (lint runs at Stop)"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -e '.stop_hook_active == true' >/dev/null 2>&1 && exit 0; uv run pyrefly check >&2 || exit 2",
            "timeout": 30,
            "statusMessage": "pyrefly type check"
          }
        ]
      }
    ]
  }
}
